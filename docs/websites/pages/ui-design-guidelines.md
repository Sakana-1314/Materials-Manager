# UI 设计规范

## 原则

| 原则 | 要求 |
| --- | --- |
| 信息优先 | 视觉层级服务于业务信息，不做无意义装饰与重复说明 |
| 页头克制 | 列表页页头只放标题与必要操作，不在标题下重复解释页面名称 |
| 一致分组 | 筛选、数据、详情、编辑统一使用卡片容器 |
| 单一主色 | 交互主色用蓝色；绿色只表达成功语义 |
| 复用优先 | 相同交互必须复用公共组件或全局样式，不复制页面级实现 |

## 设计令牌

令牌维护在 `web/src/theme.ts` 与 `web/src/styles.css`。

| 用途 | 变量 | 色值 |
| --- | --- | --- |
| 主色 | `--color-primary` | `#3f63d8` |
| 主色悬停 | `--color-primary-hover` | `#5376e4` |
| 主色浅背景 | `--color-primary-soft` | `#edf2ff` |
| 成功 | `--color-success` | `#229b6b` |
| 警告 | `--color-warning` | `#d99020` |
| 危险 | `--color-danger` | `#d94b64` |
| 强文本 | `--color-text-strong` | `#172033` |
| 正文 | `--color-text` | `#4b5565` |
| 弱文本 | `--color-text-muted` | `#7d8798` |
| 页面背景 | `--color-bg` | `#f4f6fa` |
| 卡片背景 | `--color-surface` | `#ffffff` |
| 弱化背景 | `--color-surface-soft` | `#f8faff` |
| 中性底色 | `--color-surface-muted` | `#ebedef` |
| 边框 | `--color-border` | `#e2e7ef` |
| 弱边框 | `--color-border-subtle` | `#edf0f5` |

| 项 | 值 |
| --- | --- |
| 输入框 / 选择器 / 按钮圆角 | `9px` |
| 卡片圆角 | `14px` |
| 卡片阴影 | `--shadow-card`；仅可交互统计卡片悬停用 `--shadow-card-hover` |
| 页面内容区内边距 | 上 `24px` / 左右 `28px` / 下 `32px` |
| 一级区块间距 | `18px` |
| 卡片内边距 | 由 Naive UI 主题控制，通常 `20px 22px` |
| 表单栅格列间距 / 卡片栅格间距 | `20px` / `18px` |

绿色仅用于完成、启用、正常等成功语义，不得表示普通主操作；侧栏、抽屉等需要与白卡片和浅蓝底拉开层次的区域用
`--color-surface-muted`，不用蓝色系底色。

## 页面结构

### 页头

```html
<div class="page-header">
  <div>
    <h1 class="page-title">页面标题</h1>
  </div>
  <n-space>页面操作</n-space>
</div>
```

| 场景 | 要求 |
| --- | --- |
| 列表页 | 不使用 `page-subtitle` |
| 详情页 | 标题下可显示一行关键元数据（编号、状态、时间、型号），最多一行 |
| 业务规则 | 放进对应卡片、表单提示或 `n-alert`，不写在页头 |

### 卡片与筛选区

| 项 | 要求 |
| --- | --- |
| 卡片 | 页面内容统一 `n-card`；筛选卡片加 `filter-card`（轻微蓝灰渐变），数据表卡片加 `data-card`；不在单页重设圆角/阴影/边框 |
| 简单筛选 | `filter-bar`，控件间距 `10px` |
| 多条件筛选 | 用栅格形式，保留一个“筛选条件”标题 |
| 文本搜索 | 多关键词用半角 `|` 表示“或”（兼容全角 `｜`）；同一输入框内按 OR，不同控件之间按 AND；不在占位文字里重复该规则 |
| 按钮 | 主按钮为“查询”，重置为次按钮并与查询相邻 |

### 数据表格

| 项 | 要求 |
| --- | --- |
| 表头 | 浅灰蓝背景 + 中等字重 |
| 行悬停 | 淡蓝背景，不改文字颜色 |
| 分页 | 卡片底部右侧，带顶部分隔线 |
| 操作列 | 行点击进详情时，仍保留明确文字操作或可感知的鼠标状态 |
| 列宽 | 统一引用 `web/src/constants/table.ts`，见下表 |
| 压缩控制 | 每列同时设 `width` 与相同 `minWidth`；`scroll-x` 按实际列宽计算，超宽时横向滚动，不靠等比压缩牺牲名称/型号列 |
| 普通表格 | `n-table` 必须放进 `table-scroll` 容器，并设符合字段数量的最小宽度 |

| 内容类型 | 列宽 |
| --- | --- |
| 单位、状态、数量等短字段 | `80–110px` |
| 日期、人员、编码等中等字段 | `112–176px` |
| 名称 | `220px` |
| 型号规格 | `240px` |
| 物资摘要（名称 + 型号） | `280px` |
| 用途、原因等长文本 | ≥ `200px`，并提供省略提示 |

### 详情与编辑页

| 项 | 要求 |
| --- | --- |
| 分组 | 按业务拆成多个卡片，标题用业务名词（“单据信息”“物资明细”），不用“基本信息 1” |
| 布局 | 两列用 `form-grid`；主信息 + 侧栏用 `detail-grid` |
| 返回入口 | 内容上方 `detail-toolbar` 左侧，次要按钮 + 明确返回目标，不用孤立文字链接 |
| 页头摘要 | 卡片标题已说明内容时，不再重复物资名称、编号、日期、状态 |
| 保存按钮 | 编辑表单只保留一个“保存修改”，默认在表单卡片底部右侧；顶部工具栏只放返回、删除、状态流转等页面级操作 |
| 字段保存 | 关联物资、选择单位等随整表保存，不为单个字段加独立保存或确认流程 |
| 破坏性操作 | 不得与主要保存按钮同视觉权重 |

### 顶栏账号信息

| 项 | 要求 |
| --- | --- |
| 结构 | “用户名 + 角色”两级文字，用户名为主要信息，角色用弱文本 |
| 头像 | 系统无头像能力时不显示首字母或占位头像 |
| 样式 | 默认轻量，仅悬停与键盘聚焦时用主色浅背景 + 弱边框；保留下拉的可交互状态与焦点样式 |

### 二级库信息分工

| 项 | 要求 |
| --- | --- |
| 库存查询 | 二级库菜单第一入口，集中展示当前库存、低库存状态与库存操作 |
| 物资档案 | 只展示名称、型号规格、计量单位等档案信息，列表不重复库存数量 |
| 状态设计 | 物资档案不设启用/停用状态及相应筛选、按钮、标签；操作列只保留编辑 |
| 筛选项 | 必须与当前用户权限一致，无权限的数据状态直接从选项中移除（后端同步校验） |

### 导出操作

| 项 | 要求 |
| --- | --- |
| 位置 | 页面标题右侧，不与查询/重置混排 |
| 命名 | 统一叫“导出”，具体内容由按钮标题、下拉选项与操作反馈说明 |
| 交互 | 只有一种导出方式时点击直接执行；多种方式时用同一按钮的下拉菜单承载 |
| 选项 | 明确区分“查询结果”“已选数据”与业务模板等范围；依赖勾选数据的选项在未选择时禁用 |
| 复用 | 统一用 `web/src/components/ExportButton.vue`，页面只提供选项与业务逻辑 |

## 图片附件

统一使用 `web/src/components/ImageUploader.vue`，不得在页面内新增原生文件选择器或另一套上传卡片。

| 项 | 要求 |
| --- | --- |
| 表单标签 | 统一写“图片附件” |
| 格式与数量 | JPG、PNG、WebP；单张 ≤ 10 MB；默认最多 9 张 |
| 交互 | 点击选择与 `Ctrl+V` 粘贴 |
| 组件职责 | 缩略图、删除按钮、禁用态、上传中状态由组件处理 |
| 页面职责 | 只用 `v-model:files` 绑定文件列表，不自行拼预览地址或删除逻辑 |

```html
<n-form-item label="图片附件">
  <ImageUploader v-model:files="images" />
</n-form-item>
```

## 状态与反馈

| 语义 | 颜色 |
| --- | --- |
| 主操作 | 主题蓝色 |
| 成功 / 启用 / 已完成 | 绿色 |
| 风险提醒 / 低库存 | 黄色或橙色 |
| 删除 / 失败 / 不可逆 | 红色 |
| 普通统计标签 | 中性色或主题浅色，不滥用状态色 |

加载、成功、失败反馈优先用 Naive UI 的 message、notification、dialog 与 loading。

### 加载遮罩

| 场景 | 组件与要求 |
| --- | --- |
| 局部加载 | `web/src/components/LoadingMask.vue`：模糊遮挡 + 居中 loading（可带一行文案），不用整页遮罩 |
| 宿主元素 | 遮罩范围由宿主决定（`position: absolute`），宿主需有定位上下文；`.page`、`.n-card`、`.share-table-card`、`.settings-grid` 已提供 |
| 禁用 | 禁止页面内自造遮罩样式或恢复“整块变淡”的旧做法 |
| 整页阻塞 | 仅限导出等全局操作（`ExportLoadingOverlay`） |
| 防重复提交 | 确认弹窗统一用 `web/src/composables/useImportConfirm.ts`：确认后按钮立即转圈并禁用，同时禁用取消/遮罩/Esc 关闭 |

## 实现约束

| 项 | 位置 / 要求 |
| --- | --- |
| Naive UI 主题覆盖 | `web/src/theme.ts` |
| 跨页面布局与视觉类 | `web/src/styles.css` |
| 表格列宽令牌 | `web/src/constants/table.ts` |
| 单页样式 | `<style scoped>` 只保留该页独有的布局或交互样式 |
| 颜色 | 优先用 CSS 变量或主题令牌，避免新增硬编码色值 |
| 组件复用 | 新增重复 UI 前先查 `web/src/components` 是否已有可复用组件 |
| 图标 | `NIcon` + `@vicons/ionicons5`（如 `web/src/layouts/AppLayout.vue` 侧栏），不手写内联 SVG 或字符占位；尺寸与居中交给组件库（`collapsed-icon-size` / `icon-size`），不用 margin/padding 手工微调 |

## 变更检查清单

- [ ] 页面主色、按钮、菜单选中态一致
- [ ] 列表页标题下没有可删除的重复说明
- [ ] 卡片复用全局圆角、边框、阴影
- [ ] 筛选卡片使用 `filter-card`，表格卡片使用 `data-card`
- [ ] 状态色符合语义，未用于普通装饰
- [ ] 图片附件复用 `ImageUploader`
- [ ] 图标来自 `NIcon` + `@vicons/ionicons5`，折叠态菜单图标居中
- [ ] 通过 `npm run build`、`npm run test`、`npm run lint`
