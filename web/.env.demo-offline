# 文档站内嵌的「离线演示」构建配置（gh-pages 的 /Electrical-Manager/demo/offline/）。
# 用法：vite build --mode demo-offline
#
# 与 .env.demo 的区别：后端不再走 Apifox Mock，而是用前端自带的 MSW 契约模拟
# （web/src/mocks/：真实感的固定数据 + 可交互的增删改），因此断网也能用、数据稳定可复现。
VITE_BASE_PATH=/Electrical-Manager/demo/offline/
VITE_USE_MOCK=true
